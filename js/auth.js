const register = (userData) => {
    if (!userData.length) return  false;

    console.log(`The New User Register By That Data => ${userData}`);
}

const getMe = (userToken) => {
    if (!userTokenlength) return  false;

    console.log('Your Data IS => ', userToken);
}